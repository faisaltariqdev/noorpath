import { fbEventsHandler } from "next-pixels/handlers";

/** Meta Conversions API (CAPI) + any other configured next-pixels providers */
export const POST = fbEventsHandler;

export const dynamic = "force-dynamic";
